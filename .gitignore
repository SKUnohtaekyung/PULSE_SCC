# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── 에디터 ──
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# ── 환경변수 / 시크릿 ──
# 절대 커밋하지 않는다. 공유는 .env.example 로만 한다.
.env
.env.*
!.env.example

# ── AI 도구 개인 설정 ──
# .claude/settings.json 과 .claude/rules|agents|skills 는 팀 공용이므로 커밋한다.
.claude/settings.local.json
CLAUDE.local.md
.codex/

# ── 로그 / 임시 ──
*.log
npm-debug.log*
.cache/
tmp/

# ── Node (로컬에 node 22.16.0 / npm 10.9.2 설치 확인됨) ──
node_modules/

# ── 빌드 산출물 ──
# 스택 확정 후 실제 출력 경로를 추가한다. (예: dist/, build/, .next/, out/)
# TODO: 스택 확정 시 갱신
