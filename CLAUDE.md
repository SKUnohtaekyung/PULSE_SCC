# CLAUDE.md

프로젝트 공통 계약은 `AGENTS.md` 가 정본이다. 아래 import로 항상 함께 읽는다.

@AGENTS.md

이 파일에는 **Claude Code 전용 내용만** 쓴다. `AGENTS.md` 의 내용을 여기에 복사하지 않는다.

---

## Claude 전용 구조

| 경로 | 용도 |
|---|---|
| `.claude/agents/reviewer.md` | 독립 리뷰 서브에이전트 |
| `.claude/skills/verify/` | 코드 검증 절차 |
| `.claude/skills/visual-qa/` | UI 렌더링 검증 절차 |
| `.claude/settings.json` | 팀 공용 설정 (커밋됨). `.env` 계열 읽기 차단 |

`.claude/rules/` 는 두지 않는다. Claude가 읽어야 할 규칙은 위 `@AGENTS.md` import로 이미 전부 로드되고,
같은 규칙을 rules 파일에 다시 쓰면 Codex가 읽는 `AGENTS.md` 와 갈라지기 때문이다.

`.claude/settings.local.json` 과 `CLAUDE.local.md` 는 **개인 설정**이며 `.gitignore` 대상이다. 팀 공용 설정과 혼동하지 않는다.

## 세션 시작 시

1. `git status` 와 현재 브랜치를 먼저 확인한다.
2. 진행 중 작업이 있으면 `docs/handoffs/active/` 를 확인한다.
3. 문서와 코드가 다르면 `AGENTS.md` 4장의 충돌 처리 규칙을 따른다.

## 문서 작업이 많은 저장소다

이 저장소는 코드보다 **문서·조사·발표 자료**를 더 많이 다룬다(`AGENTS.md` 1장). Claude에서 작업할 때 자주 걸리는 지점만 적는다.

**수치를 다룰 때**

- 원자료가 있으면 **원자료를 직접 파싱한다.** 요약 문서에서 옮겨 적지 않는다.
- 표를 통째로 다시 쓸 때는 **행이 사라지지 않았는지 기계로 확인한다.** 실제로 재작성 중 한 행이 지워져 합계가 어긋난 적이 있다.
- 계산해서 얻은 값은 **계산값이라고 적는다.** 공표값과 섞지 않는다.
- 통계 분류(KSIC 등)를 병기한다. 분류가 다르면 순위가 바뀐다.

**근거를 인용할 때**

- `research/rejected_sources.md` 가 **배제한 출처를 다시 쓰지 않는다.** 위키·나무위키가 여기 있다.
- 스크린샷·요약에서 읽은 것과 원문에서 확인한 것을 구분한다. 빈칸을 추론으로 메우고 사실처럼 쓰지 않는다.
- 등급(S/A/B/C/D)을 올려 적지 않는다. 간접 확인이면 그렇게 쓴다.

**파일을 옮기거나 이름을 바꿀 때**

- 이동 후 **다른 문서의 상대 링크를 전수 검사한다.** `docs/` 와 `research/` 는 서로를 많이 참조한다.

**발표 자료를 만들 때**

- 슬라이드에 올릴 것과 팀만 알아야 할 것을 구분한다. 내부 리스크 메모를 아트보드에 올리지 않는다.
- PRD `docs/product/PRD.md` §5 Non-Goals를 넘는 표현을 쓰지 않는다.
- 원본 덱(`docs/presentation/deck/`)은 커밋 대상이 아니다.

## 서브에이전트 사용

- 구현은 **한 세션이 직렬로** 수행한다. 같은 파일을 여러 에이전트가 동시에 고치지 않는다.
- `reviewer` 는 구현이 끝난 뒤 **독립 검토**에만 사용한다. reviewer가 직접 코드를 고치지 않는다.
- 읽기 전용 탐색이 넓게 필요할 때만 탐색 에이전트를 병렬로 쓴다.
- **문서 작업도 `reviewer` 검토 대상이다.** 코드가 아니라서 건너뛰지 않는다. 수치·근거·링크·내부 모순은 리뷰로만 잡힌다.

## PR 생성

규칙 정본은 `AGENTS.md` 6.2절이다. 여기에는 **Claude에서의 실행 방법만** 적는다.

1. 구현이 끝나면 먼저 `reviewer` 서브에이전트로 독립 검토를 받는다. **PASS 전에는 PR을 만들지 않는다.**
2. `.github/pull_request_template.md` 를 **Read로 실제 읽고** 그 구조대로 본문을 채운다.
   기억에 의존해 임의 형식으로 쓰지 않는다. 템플릿은 바뀔 수 있다.
3. Bash 도구에서 heredoc으로 생성한다. 명령 형태는 `AGENTS.md` 6.2절.
4. 생성 직후 라벨을 **조회해서** 확인한다. 붙지 않았으면 `gh pr edit` 로 보완한다.
5. 검증 표에는 **실제 실행한 명령과 출력**만 적는다. 실행하지 않았으면 `미실행`, 프로젝트에 없으면 `없음`.

## 아직 만들지 않은 것

`.claude/commands/`, `.claude/hooks/`, `.claude/output-styles/`, `.claude/workflows/` 는 **필요성이 확인되기 전까지 만들지 않는다.** 반복 자동화가 실제로 필요해진 시점에 추가한다.

스택이 확정되면 `.claude/launch.json` 에 dev 서버를 등록한다. Visual QA에서 실제 화면을 띄우는 데 필요하다.
