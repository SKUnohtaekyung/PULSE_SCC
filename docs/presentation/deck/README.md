# 발표 원본 파일

이 폴더에 두는 것: 발표에 실제로 쓰는 덱 원본(`.pdf`, `.pptx`, `.key`).

**커밋하지 않는다.** `.gitignore` 로 제외돼 있다. 이유는 둘이다.

- 덱에 **팀원 실명**이 들어 있고 이 저장소는 **Public** 이다
- 용량이 크다 (중간발표 덱 기준 약 35MB)

원본은 **팀 드라이브**에서 관리한다. 저장소에는 `../svg/` 의 편집 가능한 시안과 `../preview/` 만 둔다.

이 README만 커밋되어 폴더 자체는 유지된다.
